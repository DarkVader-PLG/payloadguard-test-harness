app:
  name: payloadguard-test-harness
  version: "1.0.0"
  environment: development

server:
  host: "0.0.0.0"
  port: 8080
  timeout: 30

database:
  host: localhost
  port: 5432
  name: testharness
  pool_size: 10

auth:
  session_ttl_minutes: 60
  min_password_length: 8

logging:
  level: info
  format: json
